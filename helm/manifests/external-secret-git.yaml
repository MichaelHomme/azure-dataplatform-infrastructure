apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: airflow-git-credentials
  namespace: "${AKS_AIRFLOW_NAMESPACE}"
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: SecretStore
    name: azure-store

  target:
    name: airflow-git-credentials
    creationPolicy: Owner
    template:
      data:
        GIT_SYNC_USERNAME: MichaelHomme
        GIT_SYNC_PASSWORD: "{{ .gitpat }}"
        GITSYNC_USERNAME: MichaelHomme
        GITSYNC_PASSWORD: "{{ .gitpat }}"

  data:
    - secretKey: gitpat
      remoteRef:
        key: "${AIRFLOW_GIT_PAT_SECRET_NAME}"
